#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */    
    char str[10000];
    int i,l=0;
   /* while(str[i]!='\0')
    { i=0;   
     while(str[i]!=' '&& str[i]!='\0')
     {scanf("%c",&str[i]);
     i++;
     }
    }
    while(1)
    {scanf("%s",str);
    i=strlen(str);
     if(str[i]!='')
    }
    */
    
    fgets(str,10000,stdin);
    for( i=0;str[i]!='\0';i++)
    {
        if(str[i]==' ')
            l=i+1;
    }
    printf("%d",i-l);
    return 0;
}
